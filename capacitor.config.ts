import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'message.room.com',
  appName: 'message-room',
  webDir: 'message-room',
  bundledWebRuntime: false
};

export default config;
